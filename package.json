{
  "name": "show-time",
  "version": "1.0.0",
  "description": "sends browser notifications every day about your shows that have released a new episode since the last time that you logged on.  To start, search for the names of currently airing shows to follow.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "http-server ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.7.2",
    "http-server": "^14.1.1"
  }
}
